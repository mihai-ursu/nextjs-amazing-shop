import type { NextPage } from "next";
import Layout from "../components/Layout";

const Sale: NextPage = () => {
  return (
    <Layout pageTitle="Sale">
      <h1>Sale</h1>
    </Layout>
  );
};

export default Sale;
